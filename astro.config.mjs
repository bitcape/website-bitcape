// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // The public URL of the site. Update this when the custom domain is set up.
  // Used to generate absolute URLs for SEO (canonical, Open Graph) and sitemaps.
  site: 'https://example.com',

  // Astro is a static site generator by default (output: 'static'),
  // which produces plain HTML/CSS/JS suitable for GitHub Pages.
});

